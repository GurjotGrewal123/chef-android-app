package com.example.seg2105project;

/**
 * this is an enumerator that works with account to ensure that only the three types of account can exist
 *
 */
public enum AccountType {
    ADMIN, CLIENT, COOK;
}
